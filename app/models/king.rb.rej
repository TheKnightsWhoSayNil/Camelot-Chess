diff a/app/models/king.rb b/app/models/king.rb	(rejected hunks)
@@ -12,6 +12,23 @@ class King < Piece
     false
   end
 
+  def move_to!(x,y)
+    return unless color == game.user_turn
+
+    if valid_move?(x, y) && space_available?(x, y) && not_into_check(x, y)
+      if legal_castle_move?
+        if castle!
+          game.pass_turn!(game.user_turn)
+        end
+      else
+        standard_king_move?(x, y)
+        game.pass_turn!(game.user_turn)
+      end
+    else
+      super
+    end
+  end
+
 
   def checkmate?
     # example logic
